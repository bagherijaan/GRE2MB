# GRE2MB
6TO4-GRE6-TUNNEL Script
# 🚀 **اسکریپت تونل SIT + GRE6 برای بهینه‌سازی و پایداری شبکه**

اسکریپت **GRE2MB** یک ابزار سبک و قدرتمند برای ایجاد تونل‌های دو لایه (Nested) بین دو سرور لینوکس (ایران و خارج) است. این اسکریپت با ترکیب پروتکل‌های **SIT** و **GRE6**، ترافیک را با پایداری بالا و کمترین افت سرعت عبور می‌دهد.

---
## 🛠 ویژگی‌های کلیدی
- **تونل‌زنی دو لایه:** استفاده از SIT (IPv6-in-IPv4) به عنوان لایه بیرونی و GRE6 به عنوان لایه داخلی.
- **سیستم خوددرمانی (Auto-Healing):** بررسی سلامت تونل در هر ۵ دقیقه و بازسازی خودکار در صورت قطع شدن.
- **بهینه‌سازی شبکه:** فعال‌سازی خودکار **TCP BBR**، تنظیم MSS Clamping و بهبود تنظیمات Keepalive.
- **امنیت در IPها:** تولید IPهای داخلی منحصربه‌فرد بر اساس یک `Secret Key` اختصاصی.
- **مدیریت آسان:** منوی کاملاً تعاملی برای نصب، تست اتصال و تنظیمات پورت.

## 📋 پیش‌نیازها
- **سیستم‌عامل:** Ubuntu +20.04 / Debian +11 (پیشنهادی)
- **دسترسی:** کاربر ریشه (Root)
- **ابزارهای مورد نیاز:** (معمولاً نصب هستند، اما برای اطمینان روی هر دو سرور اجرا کنید):
```bash
apt update && apt install wget curl iptables -y
```

## 🚀 نصب سریع اسکریپت

دستور زیر را کپی کرده و در ترمینال هر دو سرور (ایران و خارج) اجرا کنید:

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/bagherijaan/GRE2MB/main/gre2mb.sh)"
```



## 📖 راهنمای استفاده

نوع سرور: برای سرور واسط ایران گزینه 1 و برای سرور مقصد خارج گزینه 2 را انتخاب کنید.

آی‌پی سرور مقابل: وقتی از شما خواسته شد، IP عمومی سرور طرف مقابل را وارد کنید.

کلید مخفی: در هر دو سرور از یک Secret Key یکسان استفاده کنید تا IPها هماهنگ شوند.

فوروارد پورت: (فقط در سرور ایران) پورت‌های مورد نظر خود را برای انتقال وارد کنید.

![g2m](https://github.com/user-attachments/assets/be6a6537-b122-4ddb-b7c8-007c9b29620f)

## 📊 ساختار فنی تونل

![unnamed](https://github.com/user-attachments/assets/81b4b756-b54d-4bd4-a46f-b34ae3731d03)

در واقع این اسکریپت با ایجاد یک تونل ترکیبی (Nested Tunnel)، دیتای شما را در دو مرحله کپسوله‌سازی می‌کند؛ ابتدا پکت‌های IPv4 در لایه GRE6 قرار گرفته و سپس کل این مجموعه در بستر SIT (6to4) به صورت بسته‌های IPv6 بسته‌بندی می‌شود تا از دید سیستم‌های فیلترینگ، یک ترافیک معمولی IPv6 به نظر برسد. از ویژگی‌های بارز این اسکریپت، مدیریت هوشمند MSS Clamping است که با تنظیم دقیق سایز بسته‌ها، از قطعه‌قطعه شدن (Fragmentation) و افت سرعت ترافیک در لایه‌های تونل جلوگیری می‌کند. همچنین، برخلاف تونل‌های معمولی، این ابزار از یک Watchdog داخلی بهره می‌برد که سلامت کانال ارتباطی را با پینگ داخلی چک کرده و در صورت بروز هرگونه اختلال در سرویس یا اینترفیس‌ها، تونل را در کمتر از ۵ دقیقه به صورت خودکار بازسازی می‌کند. در بخش امنیت نیز، با استفاده از کلید اختصاصی شما و الگوریتم MD5، آی‌پی‌های داخلی به صورت داینامیک تولید می‌شوند تا از تداخل IP در شبکه‌های شلوغ جلوگیری شود. نهایتاً با فعال‌سازی پروتکل TCP BBR در هسته لینوکس، پهنای باند بهینه شده و پایداری اتصال در برابر نوسانات شدید شبکه (Packet Loss) به طرز چشم‌گیری افزایش می‌یابد.


## 👨‍💻 سازنده
Created by Bagherijaan

## 📄 لایسنس
این پروژه تحت لایسنس MIT منتشر شده است.
