# GRE2MB
6TO4-GRE6-TUNNEL Script
# 🚀 GRE2MB (نسخه ۱.۰)
**اسکریپت فوق‌العاده تونل SIT + GRE6 برای بهینه‌سازی و پایداری شبکه**

اسکریپت **GRE2MB** یک ابزار سبک و قدرتمند برای ایجاد تونل‌های دو لایه (Nested) بین دو سرور لینوکس (ایران و خارج) است. این اسکریپت با ترکیب پروتکل‌های **SIT** و **GRE6**، ترافیک را با پایداری بالا و کمترین افت سرعت عبور می‌دهد.

---

## 🛠 ویژگی‌های کلیدی
- **تونل‌زنی دو لایه:** استفاده از SIT (IPv6-in-IPv4) به عنوان لایه بیرونی و GRE6 به عنوان لایه داخلی.
- **سیستم خوددرمانی (Auto-Healing):** بررسی سلامت تونل در هر ۵ دقیقه و بازسازی خودکار در صورت قطع شدن.
- **بهینه‌سازی شبکه:** فعال‌سازی خودکار **TCP BBR**، تنظیم MSS Clamping و بهبود تنظیمات Keepalive.
- **امنیت در IPها:** تولید IPهای داخلی منحصربه‌فرد بر اساس یک `Secret Key` اختصاصی.
- **مدیریت آسان:** منوی کاملاً تعاملی برای نصب، تست اتصال و تنظیمات پورت.



## 🚀 نصب سریع

دستور زیر را کپی کرده و در ترمینال هر دو سرور (ایران و خارج) اجرا کنید:

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/bagherijaan/GRE2MB/main/gre2mb.sh)"

